import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  navWrapper: {
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'flex-start',
    position: 'absolute',
    height: '100%',
    zIndex: 1,
  },
  createListWrapper: {
    marginTop: 50,
    padding: 20,
  },
})
