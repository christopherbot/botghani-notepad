import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  navWrapper: {
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'flex-start',
    position: 'absolute',
    height: '100%',
    zIndex: 1,
  },
})
