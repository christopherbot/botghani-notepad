import react from 'react'
import { App } from '../App'

describe("App", () => {
  // const mockNavigation = {
  //   openDrawer: jest.fn(),
  //   closeDrawer: jest.fn(),
  // }

  // let app, props

  // beforeEach(() => {
  //   props = {
  //     lists: [{}],
  //     setActiveList: jest.fn(),
  //     deleteList: jest.fn(),
  //     closeModal: jest.fn(),
  //     isModalOpen: true,
  //     navigation: mockNavigation,
  //   }

  //   app = shallow(<App {...props} />)
  // })
  it("should should render the app", () => {
    // expect(App).toMatchSnapshot()
    expect(true).toBeTrue();
  })
})