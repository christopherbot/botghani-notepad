import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  textInput: {
    height:40,
    minWidth: 50,
  },
  addButton: {
    height: 40,
    minWidth: 50,
  },
  addButtonText: {
    fontSize: 30,
    lineHeight: 30,
  },
})
